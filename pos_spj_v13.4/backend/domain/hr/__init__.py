"""Human Resources bounded context — pure domain layer.

No framework, UI, or infrastructure dependencies. All identities are UUIDv7
strings from ``backend.shared.ids.new_uuid``. Monetary amounts use ``Decimal``.
"""
