"""Application-layer domain services for the Customer Master bounded
context (CRM-11+) — stateless evaluators that need more than one entity's
data but aren't full use cases (no authorization/persistence of their
own)."""
