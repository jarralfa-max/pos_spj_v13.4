# infrastructure/ — concrete implementations of persistence, messaging, and external APIs
# Repositories in this layer implement abstract interfaces from domain/ or application/.
# Legacy repositories/ directory remains for backward compatibility during migration.
