"""HR desktop module (frontend/desktop). Clean architecture UI for RRHH.

The UI never touches the database, SQL, repositories, the AppContainer or
business rules. It talks only to an already-wired ``HRPresenter``.
"""
