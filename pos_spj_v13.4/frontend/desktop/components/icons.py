"""Canonical semantic icon keys for desktop modules.

The visual theme decides how these keys are rendered. Modules should reference
these constants instead of hardcoding emoji as primary navigation or status
indicators.
"""

from __future__ import annotations


class Icons:
    HR = "hr"
    EMPLOYEE = "employee"
    ATTENDANCE = "attendance"
    PAYROLL = "payroll"
    SCHEDULE = "schedule"
    LEAVE = "leave"
    SETTINGS = "settings"
    WARNING = "warning"
    SUCCESS = "success"
    INFO = "info"
    REFRESH = "refresh"
